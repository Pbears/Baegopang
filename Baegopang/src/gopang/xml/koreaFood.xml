<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gopang.xml.koreaFood"> 
  	<select id="selectKoreaFood" resultType="brand" parameterType="java.lang.Integer">
  		SELECT DISTINCT(B.BRANDNO), BRANDNAME, PICTURE, STORENAME, 
  			LOCATION, GPA, HOURS, TEL, MINPRICE, INFO
		FROM BRAND B
        	FULL OUTER JOIN STORE S
        	ON B.BRANDNO=S.BRANDNO
		WHERE B.BRANDNO LIKE #{brandNo}||'%'
  	</select>
  	<select id="getKoreaFoodTotalRow" resultType="java.lang.Integer" parameterType="java.lang.Integer">
  		SELECT COUNT(*) FROM BRAND B
        	FULL OUTER JOIN STORE S
        	ON B.BRANDNO=S.BRANDNO
		WHERE B.BRANDNO LIKE #{brandNo}||'%'
  	</select>
</mapper>





