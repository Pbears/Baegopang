<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gopang.xml.dosirak"> 
  	<select id="selectDosirak" resultType="brand" parameterType="java.lang.Integer">
  		SELECT BRANDNO, BRANDNAME, PICTURE
  		FROM BRAND
  		WHERE BRANDNO LIKE #{brandNo}||'%'
  	</select>
  	<select id="getDodirakTotalRow" resultType="java.lang.Integer" parameterType="java.lang.Integer">
  		SELECT COUNT(*) FROM BRAND
  		WHERE BRANDNO LIKE #{brandNo}||'%'
  	</select>
</mapper>





